---
title: Layout Templates
sidebarTitle: Templates
icon: clone
description: FrontMCP UI includes pre-built layout templates for common widget patterns. These combine the base layout with structural components.
---

## Dashboard Layout

A multi-panel dashboard with stats and content areas:

```typescript
import { dashboardLayout } from '@frontmcp/ui/layouts';

const html = dashboardLayout({
  title: 'Analytics Dashboard',
  stats: [
    { label: 'Total Users', value: '12,345', change: '+12%', trend: 'up' },
    { label: 'Revenue', value: '$45,678', change: '+8%', trend: 'up' },
    { label: 'Orders', value: '892', change: '-3%', trend: 'down' },
  ],
  mainContent: chartHtml,
  sidebarContent: recentActivityHtml,
});
```

### Dashboard Options

```typescript
dashboardLayout({
  title: string,
  subtitle?: string,
  stats?: Array<{
    label: string,
    value: string | number,
    change?: string,
    trend?: 'up' | 'down' | 'neutral',
    icon?: string,
  }>,
  mainContent: string,
  sidebarContent?: string,
  actions?: string,
  theme?: ThemeConfig,
})
```

## Detail Layout

... (content continues identically to snippet in JSON above) ...
