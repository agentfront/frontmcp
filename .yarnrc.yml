# No Git-URL dependencies in this workspace — disallow them entirely (supply-chain safety).
# If a Git dependency is ever needed, list explicit repo globs here instead of "**".
approvedGitRepositories: []

enableScripts: true

nodeLinker: node-modules
