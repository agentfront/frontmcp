/**
 * Dependency injection tokens for dashboard plugin.
 */

/** Token for dashboard configuration */
export const DashboardConfigToken = Symbol('DashboardConfig');

/** Token for graph data provider */
export const GraphDataProviderToken = Symbol('GraphDataProvider');

/** Token for parent scope reference */
export const ParentScopeToken = Symbol('ParentScope');
