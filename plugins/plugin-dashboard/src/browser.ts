/**
 * @frontmcp/plugin-dashboard - Browser Entry Point
 *
 * Dashboard plugin is already browser-safe (generates HTML strings, no Node.js deps).
 * This entry re-exports everything from the main index.
 *
 * @packageDocumentation
 */

export * from './index';
